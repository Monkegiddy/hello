<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Monkegideon - Proxy</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body {
            background-color: #333;
            font-family: 'Poppins', sans-serif;
            color: white;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            overflow: hidden;
            background: linear-gradient(45deg, rgba(60, 60, 60, 0.8), rgba(100, 100, 100, 0.6));
            position: relative;
        }

        .container {
            text-align: center;
            box-shadow: 0 0 15px rgba(255, 255, 255, 0.2);
            padding: 40px;
            background: rgba(0, 0, 0, 0.7);
            border-radius: 10px;
            position: relative;
        }

        h1 {
            font-size: 3rem;
            color: #9b59b6;
            text-shadow: 0 0 10px #8e44ad, 0 0 20px #9b59b6, 0 0 30px #8e44ad;
            animation: glow 1.5s infinite alternate;
        }

        input[type="text"] {
            width: 80%;
            padding: 15px;
            font-size: 1.2rem;
            border: 2px solid #9b59b6;
            border-radius: 5px;
            background: transparent;
            color: white;
            outline: none;
            box-shadow: 0 0 10px rgba(148, 128, 255, 0.6);
            animation: glow 2s infinite alternate;
        }

        button {
            margin-top: 15px;
            padding: 15px 30px;
            font-size: 1.2rem;
            border: none;
            border-radius: 5px;
            background: #8e44ad;
            color: white;
            cursor: pointer;
            box-shadow: 0 0 15px rgba(255, 255, 255, 0.2);
            animation: bubble 1.5s infinite alternate;
        }

        button:hover {
            background: #9b59b6;
        }

        .bubbles {
            position: absolute;
            top: -20%;
            left: -20%;
            z-index: -1;
            pointer-events: none;
        }

        .bubble {
            position: absolute;
            border-radius: 50%;
            background: rgba(148, 128, 255, 0.5);
            animation: moveBubbles 10s infinite;
            opacity: 0.8;
            pointer-events: none;
        }

        .bubble1 { width: 100px; height: 100px; animation-delay: 2s; }
        .bubble2 { width: 70px; height: 70px; animation-delay: 5s; }
        .bubble3 { width: 120px; height: 120px; animation-delay: 3s; }
        .bubble4 { width: 50px; height: 50px; animation-delay: 7s; }

        @keyframes glow {
            0% { text-shadow: 0 0 10px #8e44ad, 0 0 20px #9b59b6, 0 0 30px #8e44ad; }
            100% { text-shadow: 0 0 30px #8e44ad, 0 0 40px #9b59b6, 0 0 50px #8e44ad; }
        }

        @keyframes bubble {
            0% { transform: translateY(0); }
            100% { transform: translateY(-10px); }
        }

        @keyframes moveBubbles {
            0% { top: 100%; left: 50%; }
            100% { top: -10%; left: 30%; }
        }

    </style>
</head>
<body>
    <div class="container">
        <h1>Monkegideon</h1>
        <form action="proxy" method="get">
            <input type="text" name="url" placeholder="Enter URL to Proxy" required>
            <button type="submit">Go!</button>
        </form>
    </div>

    <div class="bubbles">
        <div class="bubble bubble1"></div>
        <div class="bubble bubble2"></div>
        <div class="bubble bubble3"></div>
        <div class="bubble bubble4"></div>
    </div>
</body>
</html>
